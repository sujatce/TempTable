create or replace TABLE ASQN_DEV_QUALITY_DB.CLEANSED.EPIC_KELSEY_PATIENT_EVENT_CLNSD (
	EPIC_KELSEY_PATIENT_EVENT_CLNSD_ID NUMBER(38,0) NOT NULL DEFAULT ASQN_DEV_QUALITY_DB.CLEANSED.EPIC_KELSEY_PATIENT_EVENT_CLNSD_SEQ.NEXTVAL COMMENT 'Table''s primary key',
	EPIC_KELSEY_PATIENT_EVENT_ID NUMBER(38,0) NOT NULL COMMENT 'A unique ID from the EPIC_KELSEY_PATIENT_EVENT raw table',
	REPORT_MONTH DATE COMMENT 'Membership month from BI Analysis MRM stored in YYYY-MM-DD format',
	GMPI_ID NUMBER(38,0) COMMENT 'WellMed unique member identifier',
	PERSON_ID VARCHAR(50) NOT NULL COMMENT 'A unique identifier for a single person. The Person ID must be the same across all files.',
	SRC_TBL_ID NUMBER(15,0) COMMENT 'Source Table ID',
	SRC_TBL_NM VARCHAR(100) COMMENT 'Source Table Name',
	MEASURE_NAME VARCHAR(200) NOT NULL COMMENT 'Name of the measure for which the outcome was calculated',
	MEASURE_VERSION VARCHAR(20) COMMENT 'Version of the measure associated with the outcome. Typically a four-digit year indicating the certification edition of the measure.',
	MEASUREMENT_PERIOD_PATTERN NUMBER(1,0) COMMENT 'Calculation period used for the measure. Describes the range of data included in the outcome. Allowed values: \n    • 1=Year to Date \n    • 2=Rolling 12 Months \n     If not provided, “Year to Date” is used as the default value.',
	EVENT_DATE VARCHAR(8) COMMENT 'Date of the qualifying event which requires a subsequent follow-up visit, assessment, or monitoring for an event-based measure (yyyyMMdd).',
	LAST_RELEVANT_SERVICE_DATE VARCHAR(8) COMMENT 'Date of the last service performed to satisfy the measure (yyyyMMdd)',
	CLINICAL_DUE_DATE VARCHAR(8) COMMENT 'The date by which the next relevant clinical service is recommended (yyyyMMdd).',
	NUMERATOR_VALUE NUMBER(1,0) COMMENT 'Upper portion of the fraction or sole value used to calculate the measure score.',
	DENOMINATOR_VALUE NUMBER(1,0) COMMENT 'Lower portion of the fraction used to calculate the measure score.',
	DENOMINATOR_EXCLUSION NUMBER(1,0) COMMENT 'Count of measured entities which meet the criteria to be excluded from the denominator of the measure score before determining if the numerator criteria are met.',
	DENOMINATOR_EXCEPTION NUMBER(1,0) COMMENT 'Count of measured entities which meet the criteria to be removed from the denominator of the measure score only if the numerator criteria are not met.',
	LINE_OF_BUSINESS VARCHAR(100) COMMENT 'Name of the line of business associated with the outcome',
	BENEFIT_PLAN VARCHAR(100) COMMENT 'Name of the benefit plan associated with the outcome',
	PRODUCT_LINE VARCHAR(100) COMMENT 'Name of the product line associated with the outcome',
	EMPLOYER_GROUP VARCHAR(100) COMMENT 'Name of the employer group associated with the outcome',
	PROGRAM_ID VARCHAR(100) COMMENT 'Identifier for the program associated with the outcome',
	USER_STORY VARCHAR(25) COMMENT 'User story ID associated with data ingestion or update',
	INSERT_DATE TIMESTAMP_NTZ(7) NOT NULL DEFAULT CAST(CONVERT_TIMEZONE('America/Chicago', CAST(CURRENT_TIMESTAMP() AS TIMESTAMP_TZ(9))) AS TIMESTAMP_NTZ(7)) COMMENT 'Timestamp recording when the row was inserted',
	INSERT_USER VARCHAR(75) NOT NULL DEFAULT CURRENT_USER() COMMENT 'USER ID that first inserted data in the row',
	UPDATE_DATE TIMESTAMP_NTZ(7) NOT NULL DEFAULT CAST(CONVERT_TIMEZONE('America/Chicago', CAST(CURRENT_TIMESTAMP() AS TIMESTAMP_TZ(9))) AS TIMESTAMP_NTZ(7)) COMMENT 'Timestamp recording when the row was last updated',
	UPDATE_USER VARCHAR(75) NOT NULL DEFAULT CURRENT_USER() COMMENT 'USER ID that last updated data in the row',
	LOAD_DATE DATE NOT NULL DEFAULT CAST(CONVERT_TIMEZONE('America/Chicago', CAST(CURRENT_TIMESTAMP() AS TIMESTAMP_TZ(9))) AS DATE) COMMENT 'Date recording when the row was inserted',
	constraint PK_EK_PE_CLNSD_PRIMARYKEY primary key (EPIC_KELSEY_PATIENT_EVENT_CLNSD_ID)
)COMMENT='This table contains outcomes for patient-based and event-based measures. Each row represents the outcome of a patient-based or event-based measure in the measurement period for a patient.'
;


please check  EPIC_KELSEY_PATIENT_EVENT_CLNSD table created in cleansed schema  

data types changed to varchar(8) for date columns 

MEASUREMENT_PERIOD_PATTERN changed to Number(1,0) and description updated with bullets description


