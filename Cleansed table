CREATE OR REPLACE SEQUENCE ASQN_PRD_QUALITY_DB.CLEANSED.EPIC_KELSEY_MEDADH_CLNSD_SEQ
START = 1
INCREMENT = 1;

CREATE OR REPLACE TABLE ASQN_PRD_QUALITY_DB.CLEANSED.EPIC_KELSEY_MEDADH_CLNSD (
    EPIC_KELSEY_MEDADH_CLNSD_ID NUMBER(38,0) NOT NULL
        DEFAULT ASQN_PRD_QUALITY_DB.CLEANSED.EPIC_KELSEY_MEDADH_CLNSD_SEQ.NEXTVAL
        COMMENT 'Cleansed table primary key',

    EPIC_KELSEY_MEDADH_ID NUMBER(38,0) NOT NULL
        COMMENT 'Foreign key to RAW.EPIC_KELSEY_MEDADH',

    REPORT_MONTH DATE,
    GMPI_ID NUMBER(38,0),
    PERSON_ID VARCHAR(50) NOT NULL,
    FACT_RX_CLAIM_ID NUMBER(15,0),
    MEASURE_NAME VARCHAR(200) NOT NULL,
    MEASURE_VERSION VARCHAR(20),
    MEASUREMENT_PERIOD_PATTERN VARCHAR(30),
    PROPORTION_OF_DAYS_COVERED NUMBER(3,2),
    NUMERATOR_VALUE NUMBER(1,0),
    DENOMINATOR_VALUE NUMBER(1,0),
    DENOMINATOR_EXCLUSION NUMBER(1,0),
    DENOMINATOR_EXCEPTION NUMBER(1,0),
    IS_IMPACTABLE BOOLEAN,
    LAST_IMPACTABLE_DATE DATE,
    IS_ON_FIRST_FILL BOOLEAN,
    INDEX_PRESCRIPTION_START_DATE DATE,
    LINE_OF_BUSINESS VARCHAR(100),
    BENEFIT_PLAN VARCHAR(100),
    PRODUCT_LINE VARCHAR(100),
    EMPLOYER_GROUP VARCHAR(100),
    PROGRAM_ID VARCHAR(100),
    IS_LOW_INCOME_SUBSIDY BOOLEAN,
    LOW_INCOME_SUBSIDY_COPAYMENT_LEVEL VARCHAR(100),
    MEDICATION_NAME VARCHAR(200),
    MEDICATION_NDC VARCHAR(20),
    FIRST_FILL_DATE DATE,
    LAST_REFILL_DATE DATE,
    NEXT_REFILL_DATE DATE,
    REFILLS_REMAINING NUMBER(3,0),
    DAYS_SUPPLY NUMBER(3,0),
    ELIGIBLE_FOR_EXTENDED_DAYS_SUPPLY VARCHAR(100),
    PRESCRIBING_PROVIDER_NAME VARCHAR(200),
    PRESCRIBING_PROVIDER_NPI VARCHAR(20),
    PRESCRIBING_PROVIDER_PHONE_NUMBER VARCHAR(30),
    PHARMACY_NAME VARCHAR(200),
    PHARMACY_NPI VARCHAR(20),
    PHARMACY_PHONE_NUMBER VARCHAR(30),
    PHARMACY_DISTRIBUTION_METHOD VARCHAR(50),
    IS_PREFERRED_PHARMACY BOOLEAN,
    USER_STORY VARCHAR(25),

    INSERT_DATE TIMESTAMP_NTZ(7) NOT NULL DEFAULT CAST(CONVERT_TIMEZONE('America/Chicago', CAST(CURRENT_TIMESTAMP() AS TIMESTAMP_TZ(9))) AS TIMESTAMP_NTZ(7)),
    INSERT_USER VARCHAR(75) NOT NULL DEFAULT CURRENT_USER(),
    UPDATE_DATE TIMESTAMP_NTZ(7) NOT NULL DEFAULT CAST(CONVERT_TIMEZONE('America/Chicago', CAST(CURRENT_TIMESTAMP() AS TIMESTAMP_TZ(9))) AS TIMESTAMP_NTZ(7)),
    UPDATE_USER VARCHAR(75) NOT NULL DEFAULT CURRENT_USER(),
    LOAD_DATE DATE NOT NULL DEFAULT CAST(CONVERT_TIMEZONE('America/Chicago', CAST(CURRENT_TIMESTAMP() AS TIMESTAMP_TZ(9))) AS DATE),

    CONSTRAINT PK_EK_MEDADH_CLNSD PRIMARY KEY (EPIC_KELSEY_MEDADH_CLNSD_ID),
    CONSTRAINT FK_EK_MEDADH_CLNSD_RAW FOREIGN KEY (EPIC_KELSEY_MEDADH_ID)
        REFERENCES ASQN_PRD_QUALITY_DB.RAW.EPIC_KELSEY_MEDADH(EPIC_KELSEY_MEDADH_ID)
)
COMMENT='Cleansed table containing outcomes for Epic Kelsey medication adherence measures.';
