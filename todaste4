SELECT DISTINCT
       CAST(LOAD_DATETIME AS DATE) AS LOAD_DATE
FROM ORCHESTRATION.dbo.PILLS_ON_HAND

UNION ALL

SELECT
       CAST(NULL AS DATE) AS LOAD_DATE
WHERE NOT EXISTS
(
    SELECT 1
    FROM ORCHESTRATION.dbo.PILLS_ON_HAND
);
