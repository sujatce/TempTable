generated_body = (msg.get("generated-body") or "").lower()
template_id = msg.get("template-id")

if (
    template_id == 1788197 or
    "welcome to wellmed cares" in generated_body
):
    logging.info(
        f"[{endpoint.upper()}] Skipping Welcome template-id={template_id}"
    )
    continue
