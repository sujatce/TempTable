select
    UPPER(EMAIL_INVALID_IND) AS EMAIL_STATUS,
    count(*) as RECORD_COUNT

from  LIG_PRD_MRCS_OH_OUTREACH_SH_DB.MRCS_OH_OUTREACH.TBL_CONTACT_EMAIL EE

--where  UPPER(EE.EMAIL_INVALID_IND) = 'INVALID'

GROUP BY UPPER(EMAIL_INVALID_IND)
ORDER BY RECORD_COUNT DESC;  -- VALID 11974533 -- INVALID 286857


SELECT * FROM ASQN_PRD_MIGRATION_DB.DBO.VW_WM_UHC_MBR_EMAIL WHERE EMAIL_STATUS = 'VALID'
