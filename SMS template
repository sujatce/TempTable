Recommended SMS_TEMPLATE Table
Column	Data Type	Null?	Comments
SMS_TEMPLATE_ID	NUMBER(38,0)	NOT NULL	PK generated from sequence
TEMPLATE_ID	NUMBER(38,0)	NOT NULL	Same datatype as SMS_RESPONSE/SMS_OPT_OUT
PROGRAM_ID	NUMBER(38,0)	NOT NULL	Matches existing tables
PROGRAM_NAME	VARCHAR(255)	NOT NULL	Matches existing tables
CAMPAIGN_START_DATE	DATE	NULL	Same as SMS_RESPONSE
CAMPAIGN_END_DATE	DATE	NULL	New field
CAMPAIGN_MONTH	DATE	NOT NULL	Consistent with SMS_RESPONSE (stores month/year)
CAMPAIGN_YEAR	NUMBER(4,0)	NULL	Optional; can be derived from CAMPAIGN_MONTH
TEMPLATE_TITLE	VARCHAR(150)	NULL	Business title if available
SMS_MESSAGE	VARCHAR(2000)	NOT NULL	Complete SMS content
LANDING_PAGE_URL	VARCHAR(500)	NULL	URL extracted from SMS message
USER_STORY	VARCHAR(25)	NULL	Same as other tables
INSERT_DATE	TIMESTAMP_NTZ(7)	NOT NULL	Same default as other tables
INSERT_USER	VARCHAR(75)	NOT NULL	Same default
UPDATE_DATE	TIMESTAMP_NTZ(7)	NOT NULL	Same default
UPDATE_USER	VARCHAR(75)	NOT NULL
